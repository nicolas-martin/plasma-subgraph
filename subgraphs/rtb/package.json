{
  "name": "@subgraphs/rtb",
  "version": "2.0.4",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "goldsky subgraph deploy rtb/$npm_package_version",
    "create-local": "graph create --node http://localhost:8020/ rtb-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ rtb-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rtb-subgraph",
    "test": "graph test"
  }
}
