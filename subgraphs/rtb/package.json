{
  "name": "@subgraphs/rtb",
  "version": "2.0.4",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "goldsky subgraph deploy rtb/$npm_package_version",
    "test": "graph test"
  }
}
