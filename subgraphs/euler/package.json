{
  "name": "@subgraphs/euler",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "goldsky subgraph deploy euler/$npm_package_version",
    "test": "graph test"
  }
}
