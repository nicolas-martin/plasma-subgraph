{
  "name": "@subgraphs/gluex",
  "version": "2.0.3",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "goldsky subgraph deploy gluex/$npm_package_version",
    "test": "graph test"
  }
}
